name: tenlixor
description: Flutter SDK for Tenlixor localization platform with SharedPreferences support for offline translations.
version: 1.0.0
homepage: https://tenlixor.verbytes.com
repository: https://github.com/bbesli/Tenlixor-SDK
issue_tracker: https://github.com/bbesli/Tenlixor-SDK/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No platform-specific code
